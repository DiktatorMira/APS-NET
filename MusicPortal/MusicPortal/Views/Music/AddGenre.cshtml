@model MusicPortal.BLL.DTO.GenreDTO
@{
    ViewBag.Title = @Resources.Resource.PageTitle;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<form asp-action="AddGenre" class="forms mini">
    <h2>@Resources.Resource.GenreAddTitle</h2>
    <input asp-for="Name" class="inputs" placeholder="@Resources.Resource.GenreName" />
    <span asp-validation-for="Name" class="text-danger"></span>
    <div class="formbot">
        <a asp-action="ToGenres" class="hrefbtn inbot">@Resources.Resource.Reset</a>
        <input type="submit" value="@Resources.Resource.Add" class="btn inbot" />
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}